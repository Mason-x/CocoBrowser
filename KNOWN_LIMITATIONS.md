# Known Limitations

本文记录 0.28.2 local-fchromium 的已知限制和不能由应用单独解决的风险。

## 平台与内核

- 当前审计清单只支持 Windows x64 和 Fingerprint Chromium 148.0.7778.215。
- 不支持跨 Windows、macOS、Linux 复用同一 Persona；不同操作系统暴露面不同。
- 固定内核提升可复现性，但也意味着必须显式审计和发布新版本才能获取后续 Chromium 安全修复。
- 内核 SHA-256 验证完整性，不等同于开发者代码签名或可复现构建证明。
- 当前发布包与 Windows 可执行文件没有独立 Authenticode 证书。

## 指纹与网站风控

- 不存在“绝对不可检测”的指纹浏览器。网站可以组合 Canvas、WebGL、Audio、字体、媒体设备、Client Hints、行为轨迹、TLS、IP、DNS、账号历史和支付信息。
- Persona 一致性检查只覆盖应用已实现的字段；无法保证所有 Chromium API、GPU 驱动和扩展注入结果一致。
- 静态审计不是网页实时观测；Live 审计也只是有限采样。
- 代理出口信誉、ASN、住宅/机房属性、IP 历史和并发使用情况不受应用控制。
- 频繁更改 Persona、代理或系统环境本身可能比稳定的普通浏览器更可疑。
- 本项目不保证通过 Cloudflare、reCAPTCHA、银行、广告平台或社交平台的风控。

## 数据隔离

- 配置隔离发生在应用目录层面，不是虚拟机或操作系统沙箱。
- 同一 Windows 用户下的恶意软件、管理员、调试器或内存读取工具仍可访问运行中的数据。
- Windows 剪贴板、下载目录、文件选择器、字体、GPU、音频设备和系统服务可能形成跨配置关联。
- 多个配置同时通过同一代理、DNS、VPN 或主机网络栈会共享可观察属性。
- 便携模式只是改变数据路径，不会自动加密整个目录。

## Cookie、登录数据与迁移

- `.cocookies` 导出被密码加密，但导入到 Chromium 后仍按 Chromium 自身机制保存在配置中。
- Argon2id 参数固定用于格式兼容；未来硬件能力变化可能需要新格式版本。
- Chrome/Edge 的应用绑定 Cookie、Login Data 和系统密钥可能无法跨 Windows 用户或机器解密。
- 导入流程会主动删除扩展、后台任务、会话恢复和权限状态，因此不能完整克隆原浏览器的全部行为。
- Cookie 的 SameSite、Partitioned、CHIPS 或未来数据库字段可能随 Chromium 版本演进，旧导出不保证永久兼容。

## 扩展

- 压缩包安全验证不等于源码审计。合法 MV2/MV3 扩展仍可窃取页面、修改网络请求或连接远程服务器。
- MV2 已被 Chromium 逐步淘汰；当前允许导入不代表内核一定会继续运行它。
- 内容寻址与启动前哈希校验只能验证上传后未被替换，不能证明最初文件可信。
- 扩展自身可能改变指纹、网络头、页面脚本和自动化可检测性。

## 代理与网络

- 出口地理定位数据库可能不准确或过期，时区匹配不能证明真实物理位置。
- GeoIP 数据库尚未下载时，应用会验证代理出口可达后允许启动，并把时区一致性检查延后到数据库可用；这次启动的地理一致性状态不是完整验证。
- WebRTC、QUIC、IPv6、DNS-over-HTTPS、系统 VPN 和透明代理的组合可能产生应用未覆盖的泄漏。
- SOCKS/HTTP 代理之外的 TLS ClientHello 与底层 TCP 特征仍由内核和系统网络栈产生。
- 代理账号安全、日志策略、出口共享和服务商信誉超出本项目控制。

## 自动化

- REST API/MCP 虽只监听回环地址，但同一 Windows 用户的其他本地进程可能尝试连接；令牌必须保密。
- 启用无头模式会改变可观察行为并增加被检测概率。
- 启用 MCP JavaScript 执行等价于授予自动化客户端读取和修改当前页面的强权限。
- Agent 的点击、输入节奏、导航模式和错误处理会形成行为指纹；Persona 无法掩盖它。

## 更新与供应链

- 应用自更新已禁用，因为没有独立签名密钥；当前只能手动更新。
- `SHA256SUMS.txt` 若与 ZIP 在同一受攻击渠道发布，攻击者可能同时替换两者。
- Fingerprint Chromium 的上游源代码、二进制构建流程和发布账号是外部信任项。
- 依赖审计只覆盖扫描时已公开且数据库已收录的漏洞，不覆盖未知漏洞或恶意维护者更新。
- 2026-07-29 RustSec 扫描有 21 条维护/撤包警告，包括非 Windows GTK3/GLib 传递依赖及若干间接 crate。扫描没有报告已知漏洞，但这些警告仍需在后续升级中处理。
- pnpm 存在部分弃用/peer dependency 警告；生产漏洞扫描当前为零，不代表开发工具链永久安全。

## 兼容遗留

- 代码仍保留一部分 Wayfern、云同步和浏览器版本更新模块，以读取或迁移旧 Coco 数据；新配置和主流程不依赖它们。
- 旧 Wayfern 配置不会自动变成 Fingerprint Chromium 配置。建议新建配置并只迁移必要数据。
- 部分旧界面类型和本地文件字段为兼容序列化保留，不能视为功能承诺。
- 自托管同步代码仍在仓库中，但未纳入本地优先发布的安全验收范围；高敏感数据优先使用离线备份。

## 桌面与界面

- Tauri CSP 为 UI 样式保留 `'unsafe-inline'`，但脚本和远程连接仍受限制。
- 桌面 UI 依赖系统 WebView2；WebView2 漏洞与更新由 Microsoft 运行时决定。
- 首次下载内核约 190 MB；发布便携包默认不内嵌该内核，以保持应用包可审计和可更新。
- 当前只提供本地便携发布产物，不保证安装器、自动注册默认浏览器或企业部署策略。

## 使用边界

用户需遵守站点条款、账号授权、隐私与当地法律。该项目适合隔离测试、隐私研究和获授权的多配置工作流，不应被用来接管账号、规避访问控制或实施欺诈。
