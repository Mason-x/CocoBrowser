# Security Model

本文描述本地 Fingerprint Chromium 版本的安全目标、信任边界、已实施控制和残余风险。日期：2026-07-29。

## 1. 安全目标

本构建优先保证：

1. 不依赖商业账号、云代理或远程授权即可创建和运行本地配置。
2. 每个配置拥有独立数据目录和稳定 Persona，避免因随机参数漂移造成自相矛盾。
3. 浏览器内核、扩展、导入数据与自动化接口在进入高信任边界前都经过验证。
4. 密码、代理凭据和 Cookie 明文不会通过普通列表接口、Persona 签名或日志泄漏。
5. 默认设置不开放本地网络服务、不允许无头自动化或任意 JavaScript 执行。
6. 无法验证发布真实性时安全失败，而不是静默下载并执行更新。

本构建不承诺：

- 无法检测、匿名或可绕过任何风控系统。
- 防御拥有当前 Windows 用户权限、管理员权限或能读取进程内存的攻击者。
- 修复 Chromium、WebView2、操作系统、代理服务商或网站自身的未知漏洞。
- 将恶意扩展变成可信扩展，或替用户判断扩展业务行为。

## 2. 资产与信任边界

### 高价值资产

- 配置目录中的 Cookie、Local Storage、IndexedDB、历史记录和登录会话
- 代理用户名、密码和 WireGuard 配置
- Persona 种子与稳定指纹参数
- API/MCP 令牌及 Cookie 导出密码
- 扩展包及其授予的网站权限

### 边界

`Coco UI -> Tauri IPC -> Rust backend -> local files/processes` 是主要可信路径。Fingerprint Chromium 是独立子进程；`coco-proxy` 是每配置的本地代理子进程。REST API 和 MCP 只有用户显式启用后才成为额外入口。

互联网、代理出口、导入的浏览器目录、扩展压缩包、Cookie 文件和下载服务器均视为不可信。网站仍可观察网络、TLS、浏览器行为和浏览器暴露的全部指纹面。

## 3. 已实施控制

### 内核供应链

- 新配置固定使用 `fingerprint-chromium 148.0.7778.215`。
- 清单嵌入应用，固定平台、HTTPS URL、精确字节数和 SHA-256：
  `9ef3f471b7a6641b4224532522b29141ce3746e27d55788d88e2fd951f362579`。
- 下载限制大小，拒绝 ZIP 路径穿越与不安全条目，在暂存区校验后原子注册。
- 启动前再次检查安装登记、平台、版本、清单哈希、来源状态、目录和可执行文件。
- 当前没有可验证的独立应用签名密钥，因此应用自更新 IPC 入口被移除；只允许手动更新。

SHA-256 能发现损坏或与内置清单不符的内容，但如果攻击者同时控制源代码、构建和清单，它不等同于代码签名。

### Persona 与进程隔离

- Persona 使用密码学随机种子生成；时区、语言、UA、品牌主版本、窗口和 CPU 参数做一致性校验。
- 同一配置重复启动生成稳定参数，改变种子需要显式确认。
- 禁止危险启动参数、非回环 CDP、默认无头与 `--no-sandbox`。
- Windows Job Object 绑定自身浏览器进程树；停止配置不会按进程名误杀其他 Chromium。
- 配置单实例状态机阻止重复启动。

### 代理与网络

- 云代理对象在本地构建的启动路径中被拒绝。
- 可在启动前获取出口信息并校验 Persona 时区；代理变更会使旧地理签名失效。
- Persona 签名和日志不包含代理密码，用户名只参与不可逆摘要。
- 代理密码优先使用 Windows DPAPI 密文或环境变量引用。
- API 和 MCP 仅绑定回环地址；REST 校验 Host、Origin、请求体上限和恒定时间令牌比较。

### Cookie

- UI 的 Cookie 索引不返回值；复制与导出在后端重新读取完整记录。
- 导出格式仅为 `.cocookies`：Argon2id v19，`m=19456,t=2,p=1`，32 字节密钥，加 AES-256-GCM。
- 随机 16 字节盐、12 字节 nonce；AAD 绑定格式和 Cookie 数量。
- 导出密码至少 12 个字符。导入限制 50 MiB、100,000 条，并验证名称、值、域和路径。
- 错误密码或认证标签不匹配时拒绝解密，不输出部分明文。

### 扩展

- 仅接受 Chromium `.crx` 和 `.zip`，压缩包不超过 100 MiB。
- 限制 10,000 个条目和 500 MiB 解压体积；拒绝绝对路径、穿越、冒号、重复路径、符号链接和特殊文件。
- 根目录必须有不超过 1 MiB 的有效 MV2/MV3 `manifest.json`。
- 记录完整 SHA-256、Manifest 版本、API 权限、Host 权限和内容脚本匹配范围。
- 启动配置前重新验证包、Manifest 和哈希，再解压到内容寻址目录。

这些检查限制打包攻击和静默替换，不能证明扩展代码无恶意。权限过大的扩展仍应拒绝。

### 配置导入

- 只接受 Chrome、Edge 和 Chromium 的本地源目录。
- 源目录不能是符号链接，`Preferences` 必须是有限大小的有效 JSON 对象。
- 最多复制 200,000 个条目和 20 GiB；拒绝链接与特殊文件，目标不能位于源目录内部。
- 导入到新的 UUID 与 `Default` 目录，生成新的 Persona，不继承源浏览器指纹。
- 首次启动前移除扩展、Service Worker、后台同步、推送、会话恢复、实验开关、站点权限、代理和源下载目录。
- 任一步失败会删除本次新建 UUID 目录，不修改源目录。

### 桌面边界与存储

- Tauri CSP 默认拒绝对象、表单、框架和任意远程连接；连接只允许 IPC 和开发服务器。
- 便携模式通过可执行文件旁的 `.portable` 标记启用，数据留在包目录。
- 测试可用 `COCOBROWSER_DATA_ROOT` 将数据、缓存和日志隔离到单一目录。
- Pro 徽标、账号入口和商业授权门槛不在本地功能路径中。

## 4. 默认关闭的高风险功能

| 功能 | 默认 | 额外控制 |
|---|---:|---|
| REST API | 关闭 | 回环地址、随机令牌、Host/Origin 校验、16 MiB 请求体 |
| MCP | 关闭 | 回环地址、随机令牌 |
| 无头自动化 | 关闭 | `allow_headless_automation` 单独授权 |
| MCP JavaScript 执行 | 关闭 | `allow_js_evaluate` 单独授权 |
| 应用自更新 | 关闭且无 IPC 入口 | 手动校验包与 SHA-256 |
| 云代理 | 拒绝 | 只使用本地保存的代理 |

## 5. 依赖审计基线

2026-07-29 的发布前扫描：

- `pnpm audit --prod --audit-level high`：0 个已知漏洞。
- 实际锁定 Next 16.2.12、Sharp 0.35.0、PostCSS 8.5.18。
- `cargo-audit 0.22.2 audit`：0 个 RustSec 漏洞；21 条允许的维护/撤包警告。

Rust 警告包括仅非 Windows 目标使用的 GTK3/GLib 依赖，以及部分间接 crate 的维护状态。它们不等于已知可利用漏洞，但需要在升级 Tauri 或相关依赖时持续复核。

## 6. 安全操作建议

- 从本机受信任目录运行，并限制其他 Windows 用户访问便携目录。
- 为不同信任级别的业务使用不同 Windows 账户或虚拟机；不要只依靠浏览器配置隔离。
- 每个配置使用独立、信誉合适的代理，并先做出口与 Persona 一致性检查。
- 不导入来源不明的扩展。审阅 Host 权限和内容脚本覆盖范围。
- Cookie 导出密码应唯一，传输密码与文件时使用不同通道。
- 不要在不受信任页面打开 MCP JavaScript 执行；不使用自动化时关闭 API/MCP。
- 更新 Windows、WebView2 和本构建后重新执行指纹稳定性审计。
- 保留离线备份；删除配置或便携数据目录可能不可恢复。

## 7. 复核触发条件

出现以下任一情况应重新评估本模型：更换内核版本或下载源、增加非回环监听、启用应用自动更新、引入云同步/云代理、允许新的扩展格式、改变 Cookie 加密参数、支持新操作系统，或升级 Tauri/Chromium 主版本。
