# Coco 本地指纹浏览器使用指南

适用版本：0.28.2 dual-cloak，Windows x64。

## 1. 首次启动

### 便携包

1. 校验 ZIP 的 SHA-256 是否与同目录 `SHA256SUMS.txt` 一致。
2. 将 ZIP 完整解压到普通可写目录，例如 `D:\Apps\Coco-local`。
3. 确认 `Coco.exe`、`coco-proxy.exe` 和隐藏标记 `.portable` 位于同一目录。
4. 运行 `Coco.exe`。便携数据会写入同目录的 `data`、`cache` 和 `logs`。

不要从 ZIP 浏览器中直接运行，也不要放在会自动同步或按需释放文件的网盘目录。

### 下载浏览器内核

打开左侧“内核”页面：

- v150 最新模式：先保存一个 CloakBrowser Key，再下载当前官方版本。应用会验证官方签名清单和 ZIP 的 SHA-256。免费 Key 只允许一个活动会话。
- v146 兼容模式：无需 Key，下载固定的 146.0.7680.177.5。应用按内置大小和 SHA-256 校验。

Key 使用当前 Windows 用户的 DPAPI 加密并保存在原 CocoBrowser 数据目录；不会返回给前端。v150 启动时仍会连接 CloakBrowser 完成许可验证与会话租约。

如果提示哈希、大小或 ZIP 安全错误：

- 不要手工改登记文件或跳过校验。
- 删除本次失败的暂存文件后重新下载。
- 检查代理、杀毒软件或透明网关是否改写下载内容。

## 2. 创建配置

1. 点击“新建配置”。
2. 浏览器选择 CloakBrowser 150 最新模式或 146 兼容模式。
3. 输入有意义但不包含账号密码的名称，按业务划分分组。
4. 保持“自动 Persona”即可；只有明确知道各字段关系时才使用高级模式。
5. 保存后先不要登录账号，先完成网络与指纹审计。

系统会生成稳定 Persona。它用于同一配置的重复启动，不应在日常使用中频繁随机化。网站更容易把突然改变时区、UA、显卡特征或屏幕参数的同一会话判定为异常。

## 3. 配置代理

支持 HTTP、HTTPS、SOCKS4 和 SOCKS5。每个配置尽量使用独立出口。

1. 在“代理”中新增本地代理记录。
2. 输入主机、端口与协议；需要认证时使用凭据字段。
3. 将代理分配给目标配置。
4. 执行“匹配到出口”或地理一致性检查，让 Persona 时区和语言与出口相符。
5. 代理发生变化后重新检查；旧的地理签名会被作废。

编辑已保存代理有两个入口：

- 在“代理和 VPN”列表中点击每行明确显示的“编辑代理”按钮。
- 打开某个配置的详情 → 网络，在当前代理右侧点击“编辑代理”。

修改保存后，所有引用该代理的已停止配置都会使用新值。修改前先停止正在使用该代理的浏览器。

注意：

- 云代理对象在此本地构建中不支持。
- DNS、WebRTC、IPv6、TLS 和代理信誉都可能暴露不一致；应用内检查不能覆盖全部网络层。
- 环境变量凭据适合短期运行；DPAPI 密文通常只能在同一 Windows 用户上下文解密。

## 4. 指纹审计

打开配置详情 → 指纹，可以查看并编辑 CloakBrowser Persona 的完整受支持字段：Seed、架构版本、内核、平台、品牌版本、语言、接受语言、时区、CPU 核心数、窗口尺寸、WebRTC 策略和能力版本。运行中的配置只读；停止浏览器后才能保存。Canvas/WebGL 默认保持稳定 seed 噪声；只有明确需要关闭时才勾选全局关闭项。

打开配置的“身份/Persona”，选择“指纹审计”。

- 单次审计比较配置与浏览器实际暴露值，包括时区、UA、webdriver 和常见 WebRTC 泄漏。
- 稳定性审计连续运行多轮并比较哈希，用于发现启动间漂移。
- Live 采集不可用时会降级为静态审计，结果中会明确标识，不能与真实页面采集等价。

推荐在以下时间重新审计：

- 第一次使用配置
- 更换代理、内核或 Persona
- Windows、WebView2 或显卡驱动大版本更新
- 安装会修改页面或网络行为的扩展后

“通过”只表示已检查项目内部一致，不代表任何网站都会接受该配置。

## 5. Cookie 管理

### 查看与复制

界面列表只展示 Cookie 索引，不把值传到 WebView。后端复制时才读取完整值。只有停止的 Persona 内核环境可作为稳定的数据操作目标。

### 导出

1. 打开 Cookie 管理并选择导出。
2. 输入至少 12 个字符的独立密码并确认。
3. 保存 `.cocookies` 文件。

文件使用 Argon2id 和 AES-256-GCM 加密。忘记密码无法恢复；不要把密码写在文件名或同目录文本中。

### 导入

选择 `.cocookies` 并输入正确密码。导入上限为 50 MiB 和 100,000 条。错误密码、损坏文件或不合法的域/路径会被拒绝。

请只导入你有权使用的会话数据。Chrome 的应用绑定加密数据可能无法在不同机器或用户中解密。

## 6. 扩展

扩展管理只接受 `.crx` 和 `.zip`。

上传后检查：

- Manifest 版本是 MV2 还是 MV3
- SHA-256 是否与预期文件一致
- API 权限是否包含 `cookies`、`webRequest`、`proxy`、`debugger` 等高权限
- Site access 是否覆盖 `<all_urls>` 或不必要的域

应用会阻止路径穿越、符号链接、异常体积和启动前静默替换，但不会审计扩展 JavaScript 的业务逻辑。来源不明或权限过大的扩展不要加载。

## 7. 导入 Chrome、Edge 或 Chromium 配置

1. 完全退出源浏览器，避免数据库仍在写入。
2. 在“导入配置”中选择 Chrome、Edge 或 Chromium。
3. 选择具体配置目录；目录根部必须有有效 `Preferences`。
4. 可选分配本地代理，开始导入。

导入会复制到新 UUID，放入新配置的 `Default` 目录并创建全新 Persona。为降低持久化攻击与跨配置污染，以下内容不会继承：扩展、Service Worker、后台同步、推送、会话恢复、站点权限、代理、实验开关和源下载目录。

源目录不会被修改。导入失败时，新建目标会回滚。

## 8. 本地 API 与 MCP

打开“设置 → 集成”后才能启用。

### REST API

- 默认端口 10108，仅监听回环地址。
- 启用后保存随机令牌；请求必须携带正确令牌。
- 不要通过端口转发、反向代理或防火墙规则暴露到局域网/公网。

### MCP

- 仅回环监听并使用令牌。
- “允许无头自动化”与“允许 JavaScript 执行”是两个独立高风险开关。
- JavaScript 执行可读取当前页面可访问的数据；完成任务后立即关闭。

如果不需要 Agent 或自动化，保持所有集成开关关闭。

## 9. 备份与迁移

先停止全部配置并退出 Coco，再复制完整便携目录，或普通安装的数据目录。推荐：

1. 备份前检查没有 `Coco.exe`、`chrome.exe` 或 `coco-proxy.exe` 进程。
2. 对备份压缩包做加密并记录 SHA-256。
3. 在隔离位置做一次恢复演练。

DPAPI 密文、Chrome Cookie 数据库和登录数据可能绑定原 Windows 用户或设备。迁移到另一台机器前应准备重新输入代理密码和重新登录。

## 10. 手动更新

当前应用没有可信的代码签名密钥与签名发布清单，因此自更新已关闭。

安全更新流程：

1. 备份数据。
2. 获取新发布包和独立发布的 SHA-256。
3. 在可信终端运行 `Get-FileHash -Algorithm SHA256 <file>`。
4. 校验后解压到新目录，不覆盖正在使用的旧目录。
5. 复制数据或使用原便携数据的备份副本进行首次启动。
6. 重新执行指纹稳定性审计，确认无回归后再停用旧版本。

只把 ZIP 与同一下载位置的 `SHA256SUMS.txt` 一起校验，不能防御下载站同时被篡改；最好通过独立可信渠道保存期望哈希。

## 11. 故障排查

- `KERNEL_NOT_INSTALLED`：在“内核”下载固定版本，或重新验证安装登记。
- `GEO_TIMEZONE_MISMATCH`：更新 Persona 时区，或换回与 Persona 一致的出口。
- `PROXY_EXIT_CHECK_FAILED`：检查代理可用性、认证和本地 sidecar。
- 缺少 GeoIP 数据库：应用会后台下载并允许已验证出口 IP 的浏览器先启动，只延后时区一致性校验；“匹配到出口”仍需等待数据库下载完成。
- `COOKIE_EXPORT_DECRYPT_FAILED`：密码错误或包已损坏，不会部分恢复。
- `EXTENSION_PACKAGE_INVALID`：检查格式、根 Manifest、文件名、体积和哈希。
- `IMPORT_SOURCE_INVALID`：退出源浏览器，选择包含有效 `Preferences` 的真实目录。

日志位于 `logs` 目录。分享日志前检查路径、域名、配置名和网络地址；程序会主动脱敏常见凭据，但日志仍可能包含使用环境信息。

## 12. 清理

便携模式完整卸载：

1. 停止所有配置并退出 Coco。
2. 确认相关进程结束。
3. 先备份需要的 `.cocookies`、配置数据或审计结果。
4. 删除整个便携目录。

删除后，未备份的数据不可恢复。程序不会自动删除目录外的源浏览器数据。
